import React from 'react';
import { Layout } from 'antd';

const Header = () => {

  return (
      <React.Fragment>
    <header id="home">
            hi
    </header>
    </React.Fragment>
  );
};

export default Header;
